import { SectionAnimDirective } from './section-anim.directive';

describe('SectionAnimDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionAnimDirective();
    expect(directive).toBeTruthy();
  });
});
